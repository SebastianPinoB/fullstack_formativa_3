import { Typography } from "antd";
import React from "react";
import Formulario from "../atoms/formulario";

const {Title} = Typography

export default function CrearEnvio(){
   return(
      <Formulario />
   )
}